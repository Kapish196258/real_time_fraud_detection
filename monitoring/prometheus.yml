global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: spark-fraud-pipeline
    metrics_path: /metrics
    static_configs:
      - targets:
          - host.docker.internal:8000
        labels:
          application: real-time-fraud-detection
          component: spark-streaming